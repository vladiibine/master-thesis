THe application is started by starting the module 'guy.py' in the operating system specific way.

In Windows, if Python 2.7 is installed, and wxPython 2.8, the command
'python gui.py' from this directory would start the application.

In Linux, the same command and restrictions as in Windows apply.